<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\DTMS.Fleet.Domain\DTMS.Fleet.Domain.csproj" />
    <ProjectReference Include="..\DTMS.Fleet.IntegrationEvents\DTMS.Fleet.IntegrationEvents.csproj" />
    <ProjectReference Include="..\..\..\DTMS.SharedKernel\DTMS.SharedKernel.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="MassTransit" />
  </ItemGroup>

  <ItemGroup>
    <!-- Command handlers are internal sealed (module convention); the unit tests
         exercise them directly rather than through MediatR. Same arrangement as
         DTMS.Facility.Application ↔ Facility.UnitTests. -->
    <InternalsVisibleTo Include="Fleet.UnitTests" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
